<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" " http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiyan.dao.master.CheckMasterDao">
    <insert id="insertCheck" parameterType="com.xiyan.model.entrty.Check">
        insert into check(
        check_id,
        check_user,
        check_time,
        check_result,
        check_type
        )
        values(
        #{checkId},
        #{checkUserId},
        #{checkTime},
        #{checkResult},
        #{checkType}
        )

    </insert>
</mapper>